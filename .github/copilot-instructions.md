

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v6.10.11 -->
<!-- Updated: 2026-05-22T20:28:30.447Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

## SigMap commands

| When | Command |
|------|---------|
| Before answering a question about code | `sigmap ask "<your question>"` |
| After changing config or source dirs | `sigmap validate` |
| To verify an AI answer is grounded | `sigmap judge --response <file>` |

# Code signatures

## deps
```
packages/frontend/src/hooks/useFleet.js ← useSocket
```

## changes (last 10 commits — 2 days ago)
```
packages/frontend/src/hooks/useFleet.js       +useFleet  ~useFleet
packages/monitor-agent/src/agent.js           ~main
```

## packages

### packages/frontend/src/components/MultiCityComparison/MultiCityComparison.css
```
.multi-city-comparison
.comparison-header
.comparison-header
.comparison-label
.city-cards
.city-card
.city-card
.card-header
```

### packages/frontend/src/hooks/useFleet.js
```
export function useFleet(selectedCity = 'ams')
```

### packages/monitor-agent/src/agent.js
```
async function loadOrBuildGraph()
async function main()
```

### packages/router-agent/src/agent.js
```
module.exports = { optimizeRoute, getGraph }
function getGraph()
async function optimizeRoute({ originId, destinationId, vehicle, trace = false })
```

### packages/router-agent/src/algorithms/__tests__/astar.test.js
```
function buildTestGraph()
```

### packages/router-agent/src/algorithms/astar.js
```
module.exports = { aStar, costFunction, heuristic, haversineKm }
function costFunction(edge, vehicle) → number
function haversineKm(a, b) → number
function toRad(deg)
function heuristic(node, destination, gnnScalar = 1.0) → number
function reconstructPath(cameFrom, current) → { nodes: object[], edges:
function aStar(graph, origin, destination, vehicle, options = {}) → { nodes, edges, segments,
```

### packages/router-agent/src/algorithms/carbonPricing.js
```
module.exports = { EMISSION_FACTORS, DEFAULT_CARBON_PRICE_PER_TONNE, segmentCarbonCost, routeCarbonCost, computeCo2Savings }
function segmentCarbonCost(distanceKm, vehicleType, carbonPricePerTonne = DEFAULT_CARBON_PRICE_PER_TONNE) → { co2Kg: number, shadowCo
function routeCarbonCost(segments, vehicleType, carbonPricePerTonne = DEFAULT_CARBON_PRICE_PER_TONNE) → { totalCo2Kg: number, tot
function computeCo2Savings(baselineSegments, optimizedSegments, vehicleType) → { savedCo2Kg: number, sav
```

### packages/router-agent/src/algorithms/carbonQuantification.js
```
module.exports = { quantifyCarbonSavings, generateEquivalencies, generateImpactStatement, projectAnnualImpact, calculateFleetImpact, EQUIVALENCIES, CARBON_PRICE_EU_ETS_PER_TONNE }
function quantifyCarbonSavings(co2SavedKg) → object
function generateEquivalencies(co2SavedKg) → object
function generateImpactStatement(co2SavedKg) → string
function projectAnnualImpact(dailyCO2SavedKg, daysPerYear = 365) → object
function calculateFleetImpact(routesList) → object
```

### packages/router-agent/src/algorithms/dijkstra.js
```
module.exports = { dijkstra }
function dijkstra(graph, origin, destination, vehicle) → { nodes, edges, segments
```

### packages/router-agent/src/algorithms/minHeap.js
```
class MinHeap
  constructor(comparator)
  size() → number
  isEmpty() → boolean
  push(value)
  pop() → T
  if(this._heap.length > 0)
  peek() → T
  while(i > 0)
module.exports = { MinHeap }
```

### packages/router-agent/src/fleet/replanner.js
```
class FleetReplanner
  constructor({ graph, redis, fleet })
  async executeReplan(instruction) → Promise<{ routesUpdated: 
  for(const vehicleId of vehicleIds)
  if(result)
  if(this._redis && results.length > 0)
  if(!vehicle)
  if(constraints.avoidSegment)
  if(!result)
module.exports = { FleetReplanner }
```

### packages/router-agent/src/gnn/heuristicClient.js
```
module.exports = { initializeGNNHeuristic }
async function initializeGNNHeuristic(origin, destination) → Promise<function>
```

### packages/router-agent/src/graph/GraphStore.js
```
class GraphStore
  constructor()
  addNode(node)
  addEdge(fromId, edge)
  addUndirectedEdge(aId, bId, edgeProps)
  getNode(id) → GraphNode | undefined
  edgesFrom(fromId) → GraphEdge[]
  nodeCount() → number
  edgeCount() → number
module.exports = { GraphStore }
```

### packages/router-agent/src/graph/builder.js
```
module.exports = { buildGraph, loadGraph, parseGtfsFile }
function parseCsvLine(line) → string[]
async function parseGtfsFile(filePath) → Promise<{ headers: string
async function buildGraph({ gtfsDir, outputPath, maxStops } = {}) → Promise<GraphStore>
async function addStopTimeEdges(graph, stopTimesFile, routeTypeMap)
function addProximityEdges(graph)
function loadGraph(cachePath) → GraphStore
```

### packages/frontend/Dockerfile
```
FROM node:20-alpine AS builder
FROM nginx:alpine
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
ENV VITE_API_URL
ARG VITE_API_URL
```

### packages/frontend/README.md
```
h1 GreenRoute AI — Frontend
h2 Overview
h2 Stack
h2 Component Reference
h3 `LiveMap/`
h3 `CO2Ticker/`
h3 `FleetDashboard/`
h3 `AlgoTrace/`
h3 `BeforeAfter/`
h3 `Leaderboard/`
h3 `ReplanBanner/`
h2 Hooks
h3 `useSocket.js`
h3 `useFleet.js`
h2 Local Development
h1 From repo root
h1 Start the full stack (API gateway + agents + infra)
h1 Start the frontend dev server (hot reload)
h1 → http://localhost:5173
h2 Production Build
h1 Output: packages/frontend/dist/
h2 Environment Variables
code-fence bash
code-fence plain
code-fence ---
```

### packages/frontend/index.html
```
title: GreenRoute AI — AlgoFest 2026
div#root
```

### packages/frontend/src/App.css
```
.counter
.hero
.ticks
```

### packages/frontend/src/components/CO2Ticker/CO2Ticker.jsx
```
function AnimatedCount({ value, decimals = 3 })
```

### packages/frontend/src/components/CitySelector/CitySelector.css
```
.city-selector
.city-selector-header
.city-selector-header
.city-badge
.city-buttons
.city-btn
.city-btn
.city-btn
```

### packages/frontend/src/components/FleetDashboard/FleetDashboard.jsx
```
function VehicleCard({ vehicle, route, isSelected, onClick })
```

### packages/frontend/src/components/MultiCityComparison/MultiCityComparison.jsx
```
function MetricBar({ label, value, unit, color = '#10b981', max = null })
```

### packages/frontend/src/hooks/useSocket.js
```
export function useSocket(handlers = {}) → { connected: boolean, emi
function getSocket()
```

### packages/frontend/src/index.css
```
var --color-bg
var --color-bg-2
var --color-bg-3
var --color-surface
var --color-surface-alt
var --color-border
var --color-border-glow
var --color-green-400
var --color-green-500
var --color-green-600
var --color-green-glow
var --color-amber
var --color-red
var --color-blue
var --color-purple
var --color-text-primary
var --color-text-secondary
var --color-text-muted
var --radius-sm
var --radius-md
var --radius-lg
var --radius-xl
var --shadow-glow
var --shadow-card
var --transition
```

### packages/monitor-agent/src/analytics/trafficPredictor.js
```
module.exports = { calculateEMA, predictTraffic, generateRecommendation, predictMultipleSegments }
function calculateEMA(speeds, alpha = 0.3) → number
function predictTraffic(cityId, currentHour, recentSpeeds = []) → object
function generateRecommendation(forecast) → string
function predictMultipleSegments(cityId, currentHour, segments = []) → object[]
```

### packages/monitor-agent/src/anomaly/detector.js
```
class RunningStats
  constructor(windowSize = DEFAULT_WINDOW_SIZE)
  push(value)
  if(this._window.length >= this._windowSize)
  count() → number
  mean() → number
  stdDev() → number
  zScore(value) → number
  isReady() → boolean
class AnomalyDetector
  constructor({ windowSize = DEFAULT_WINDOW_SIZE, zThreshold = DEFAULT_Z_THRESHOLD, minSpeedDropPct = DEFAULT_MIN_SPEED_DROP_PCT } = {})
  observe(segmentKey, observedSpeedKmh, freeFlowSpeedKmh) → AnomalyEvent | null
  if(stats.isReady)
  if(z < -this._zThreshold && speedDrop >= this._minSpeedDropPct)
  segmentCount() → number
module.exports = { AnomalyDetector, RunningStats }
```

### packages/monitor-agent/src/cities/cityConfig.js
```
module.exports = { CITIES, getCity, listCities }
function getCity(cityKey) → object|null
function listCities() → object[]
```

### packages/monitor-agent/src/cities/trafficSimulator.js
```
module.exports = { simulateSpeed, shouldTriggerSpike, generateTrafficSpike, estimateArrivalTime, SPEED_VARIANCE, WEEKEND_MULTIPLIER }
function simulateSpeed(cityId, freeFlowSpeedKmh, hour, dayOfWeek) → number
function shouldTriggerSpike(cityId) → boolean
function generateTrafficSpike(cityId) → object
function estimateArrivalTime(distanceKm, currentSpeedKmh) → number
```

### packages/monitor-agent/src/monitors/co2Monitor.js
```
class Co2Monitor
  constructor({ redis } = {})
  updateVehicle(vehicleId, vehicleType, segmentDistKm, currentSpeedKmh)
  recordSavings(savedKg, savedUsd)
  start()
  stop()
  if(this._timer)
  for(const [, { vehicleType, currentSpeedKmh }] of this._activeVehicles)
  if(this._redis)
module.exports = { Co2Monitor }
```

### packages/monitor-agent/src/monitors/trafficMonitor.js
```
class TrafficMonitor
  constructor(graph, options = {})
  if(SIM_MODE)
  start()
  stop()
  if(this._timer)
  for(const { fromId, toId, speedKmh, freeFlowSpeedKmh } of updates)
  if(anomaly)
  for(let i = 0; i < sampleSize; i++)
module.exports = { TrafficMonitor }
```

### packages/api-gateway/src/index.js
```
async function startRedisRelay()
```

### packages/api-gateway/src/middleware/errorHandler.js
```
module.exports = { errorHandler }
function errorHandler(err, req, res, next)
```

### packages/api-gateway/src/services/carbonCertificate.js
```
module.exports = { generateCertificate, certificateToCSVRow, certificateCSVHeader, generateTextReport, generateFleetReport }
function generateCertificate(route, cityId) → object
function certificateToCSVRow(certificate) → string
function certificateCSVHeader() → string
function generateTextReport(certificate) → string
function generateFleetReport(certificates, cityId) → string
```

### packages/replanner-agent/src/agent.js
```
async function main()
```

### packages/replanner-agent/src/decision/replanDecider.js
```
class ReplanDecider
  constructor({ redis, subscriber, graph, fleet })
  async start()
  if(channel === 'greenroute:replan-needed')
  if(now - this._lastReplanAt < REPLAN_COOLDOWN_MS)
  if(anomaly.speedDropPct < REPLAN_THRESHOLD_PCT)
  if(!decision)
  if(decision.vehicleIds.length === 0 && decision.priority === 'low')
module.exports = { ReplanDecider }
```

### packages/replanner-agent/src/gemini/client.js
```
module.exports = { askGemini, USE_GEMINI, MODEL_NAME }
function getModel()
async function askGemini(prompt) → Promise<ReplanDecision>
function parseGeminiResponse(text) → ReplanDecision | null
function sleep(ms)
```

### packages/replanner-agent/src/gemini/prompts.js
```
module.exports = { buildReplanPrompt, SYSTEM_PROMPT }
function buildReplanPrompt({ anomaly, fleet, graph }) → string
```

### packages/api-gateway/Dockerfile
```
FROM node:20-alpine
EXPOSE 3000
CMD ["node", "packages/api-gateway/src/index.js"]
ENV PORT
ENV NODE_ENV
```

### packages/monitor-agent/Dockerfile
```
FROM node:20-alpine
CMD ["node", "packages/monitor-agent/src/agent.js"]
ENV NODE_ENV
```

### packages/replanner-agent/Dockerfile
```
FROM node:20-alpine
CMD ["node", "packages/replanner-agent/src/agent.js"]
ENV NODE_ENV
```

### packages/router-agent/Dockerfile
```
FROM node:20-alpine
CMD ["node", "packages/router-agent/src/agent.js"]
ENV NODE_ENV
```
